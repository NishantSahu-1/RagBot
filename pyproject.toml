[project]
name = "genai-projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nishant Sahu", email = "nishant.sahu@example.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
genai-projects = "genai_projects:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
